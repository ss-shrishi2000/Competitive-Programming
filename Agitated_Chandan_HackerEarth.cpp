#include<bits/stdc++.h>
using namespace std;
vector<pair<long long, long long>> v[100005];
long long dist[100005], vis[100005];
void check()
{
    for (long long i=0;i<100005;i++)dist[i]=INT_MAX;
}
void bfs(long long source)
{
    queue<long long>q;
    q.push(source);
}
int main()
{
    int t;cin>>t;
    while(t--)
    {
        int n;cin>>n;
        check();
    }
}
